<Window x:Class="StudentAssistantApp.Views.LoginFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentAssistantApp.Views"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" ResizeMode="NoResize"
        Title="LoginFormView" Height="600" Width="400" 
        WindowStartupLocation="CenterScreen"
        >
    <Grid>
        <StackPanel Margin="40">
            <Label Content="Student Assistant" FontSize="20" HorizontalAlignment="Center" Margin="0, 0"/>
            <StackPanel Margin="20, 40">
                <TextBox Margin="10, 20"/>
                <TextBox Margin="10, 20"/>
            </StackPanel>
            <StackPanel Margin="0 40 0 0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="Close" Content="Close" Margin="15, 0"/>
                    <Button x:Name="LogIn" Content="Log In" Margin="15, 0"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
